replicaCount: 1

image:
  repository: ghcr.io/gopaytech/fortigate_exporter
  tag: latest
  pullPolicy: IfNotPresent

fortigate:
  address: "FORTIGATE_IP_ADDRESS"
  apiKey: "FORTIGATE_API_TOKEN"  # You will override this with secrets in production

service:
  type: ClusterIP
  port: 9182

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}
